import java.util.LinkedList;
import java.util.Queue;
import java.util.Stack;

public class QueueStandProblem2 {
    public static void main(String[] args) {
        Queue<String> name=new LinkedList<>();
        /*Stack<String> name1=new Stack<>();
        name.offer("D");
        name.offer("A");
        name.offer("R");
        name.offer("S");
        name.offer("H");
        name.offer("A");
        name.offer("N");
        System.out.println(name);
        while (!name.isEmpty()){
            name1.push(name.poll());
        }
        while (!name1.isEmpty()){
            name.offer(name1.pop());
        }
        System.out.println(name);*/
        name.offer("Darshan");

        System.out.println(name);

        while (!name.isEmpty()){

        }
    }
}
